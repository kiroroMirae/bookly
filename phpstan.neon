includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app/

    level: 6

    # Raise toward 8 as the project matures; don't start there — the noise
    # buries real findings in a young codebase.
